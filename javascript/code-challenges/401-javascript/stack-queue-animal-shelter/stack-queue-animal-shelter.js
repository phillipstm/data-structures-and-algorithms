'use strict';

const { Queue } = require('../');

class Animal {
  constructor(name, type) {
    this.name = name;
    this.type = type;
  }
}
class AnimalShelter {
  constructor() {
    this.dog = new Queue();
    this.cat = new Queue();
  }

 enqueue(animal) {
    if pref = this.dog.queue(),
    
  }
}

